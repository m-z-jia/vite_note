# 随心记属性解释

本文档详细解释随心记应用的各项配置属性，帮助用户理解和自定义应用功能。

## 基本设置

### 常规

- **自动保存**：启用后，编辑内容会自动保存，无需手动点击保存按钮
- **启动时打开上次文件**：应用启动时自动打开上次关闭时的文件
- **新建文件默认路径**：设置新建文件的默认保存位置
- **默认文件扩展名**：设置新建文件的默认扩展名，如 `.md`、`.txt` 等
- **自动换行**：启用后，文本会根据窗口宽度自动换行
- **显示行号**：在编辑器左侧显示行号
- **显示空白字符**：显示空格、制表符等空白字符
- **显示行尾符**：显示行尾的换行符
- **启用拼写检查**：启用拼写检查功能，标记可能的拼写错误

### 编辑器

- **字体**：设置编辑器字体
- **字体大小**：设置编辑器字体大小
- **行高**：设置编辑器行高
- **Tab 宽度**：设置 Tab 键的缩进宽度
- **使用空格代替 Tab**：启用后，按 Tab 键时插入空格而非 Tab 字符
- **缩进指南**：显示代码缩进的辅助线
- **高亮当前行**：高亮显示当前编辑行
- **高亮匹配括号**：当光标位于括号附近时，高亮显示匹配的括号

## 界面设置

### 外观

- **主题**：选择应用主题，如浅色、深色等
- **图标主题**：选择应用图标主题
- **工具栏**：设置工具栏的显示方式和内容
- **状态栏**：设置状态栏的显示内容
- **侧边栏**：设置侧边栏的显示方式和内容
- **标签栏**：设置标签栏的显示方式

### 布局

- **编辑器布局**：设置编辑器的布局方式，如单列、双列等
- **预览布局**：设置预览窗口的布局方式
- **面板位置**：设置各种面板的位置
- **分割比例**：设置分割窗口的比例

## 快捷键

### 常用快捷键

- **新建文件**：创建新文件的快捷键
- **打开文件**：打开文件的快捷键
- **保存文件**：保存文件的快捷键
- **另存为**：另存为的快捷键
- **关闭文件**：关闭文件的快捷键
- **撤销**：撤销操作的快捷键
- **重做**：重做操作的快捷键
- **复制**：复制文本的快捷键
- **剪切**：剪切文本的快捷键
- **粘贴**：粘贴文本的快捷键
- **全选**：全选文本的快捷键
- **查找**：查找文本的快捷键
- **替换**：替换文本的快捷键
- **查找下一个**：查找下一个匹配项的快捷键
- **查找上一个**：查找上一个匹配项的快捷键

### 高级快捷键

- **切换侧边栏**：显示/隐藏侧边栏的快捷键
- **切换状态栏**：显示/隐藏状态栏的快捷键
- **切换工具栏**：显示/隐藏工具栏的快捷键
- **切换预览**：显示/隐藏预览窗口的快捷键
- **格式化文档**：格式化文档的快捷键
- **注释/取消注释**：注释或取消注释代码的快捷键
- **缩进**：增加缩进的快捷键
- **减少缩进**：减少缩进的快捷键
- **向上移动行**：向上移动当前行的快捷键
- **向下移动行**：向下移动当前行的快捷键
- **复制行**：复制当前行的快捷键
- **删除行**：删除当前行的快捷键

## 高级设置

### 文件关联

- **关联文件类型**：设置应用关联的文件类型
- **默认打开方式**：设置文件的默认打开方式

### 性能

- **文件大小限制**：设置可以打开的文件大小限制
- **内存使用限制**：设置应用的内存使用限制
- **自动备份**：设置自动备份的频率和位置

### 网络

- **代理设置**：设置网络代理
- **自动更新**：设置应用自动更新的方式

### 安全

- **密码保护**：设置应用密码保护
- **隐私设置**：设置应用的隐私选项

## 关于

### 应用信息

- **版本**：显示应用版本信息
- **版权**：显示应用版权信息
- **许可证**：显示应用许可证信息

### 帮助

- **文档**：打开应用文档
- **教程**：打开应用教程
- **反馈**：提交反馈和建议
- **社区**：访问应用社区

### 更新

- **检查更新**：手动检查应用更新
- **更新历史**：查看应用更新历史

## 总结

通过上述配置项，用户可以根据自己的需求和偏好自定义随心记应用的各项功能，提高使用体验和工作效率。
